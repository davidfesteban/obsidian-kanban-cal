{
  "name": "obsidian-kanban-cal",
  "version": "0.1.0",
  "private": true,
  "description": "Obsidian task-list kanban plugin",
  "scripts": {
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.23.0",
    "obsidian": "^1.6.5",
    "typescript": "^5.5.3"
  }
}
